# Optional: local semantic memory (index_memory / search_memory tools).
# Install alongside requirements.txt: pip install -r requirements.txt -r requirements-memory.txt
# Downloads a small (~80MB) embedding model on first use, runs fine on a Pi 4/5.
sentence-transformers>=3.0.0
