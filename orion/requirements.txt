anthropic>=0.40.0
# anthropic depends transitively on jiter and pydantic-core (both Rust/PyO3).
# No prebuilt wheel exists for Termux/Android, so pip compiles them from
# source there — run `pkg install rust binutils` in Termux BEFORE this
# install or it fails with "Failed to build 'jiter'". See README §11.
python-dotenv>=1.0.0

# Interfaces (job-queue extra powers proactive reminder delivery)
python-telegram-bot[job-queue]>=21.0
# Voice replies on Telegram also need the `ffmpeg` system binary (not a pip
# package) to encode audio into the Opus format Telegram voice notes require —
# install via your OS package manager (apt/brew/...).

# Google integrations (Gmail + Calendar)
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Home Assistant, web tools, info tools
httpx>=0.27.0
beautifulsoup4>=4.12.0

# Utility tools (QR codes, system status)
qrcode[pil]>=7.4.0
# psutil refuses to install on Android/Termux ("platform android is not
# supported") — if you hit that there, comment this line out and reinstall;
# tools/system_tool.py degrades gracefully without it (see its comments).
psutil>=5.9.0

# Office documents (PowerPoint / Word / Excel), create + edit by voice or text
python-pptx>=0.6.23
python-docx>=1.1.0
openpyxl>=3.1.0

# Media: Spotify control, YouTube search, casting to Chromecast/Google TV
# (also drives launching Netflix/Disney+/YouTube Music on the TV — see README
# for the real capabilities vs. limitations of each)
spotipy>=2.24.0
PyChromecast>=14.0.0
youtube-transcript-api>=0.6.2

# Calendars/tasks/notes beyond Google (CalDAV covers Outlook/iCloud/Nextcloud)
caldav>=1.3.0
feedparser>=6.0.10

# Documents: read existing PDF/Word files (creating them uses python-pptx/docx/openpyxl above)
pypdf>=4.0.0

# Network diagnostics
speedtest-cli>=2.1.3

# Ambient sound synthesis (white/pink/brown noise) — also used by the voice interface
numpy>=1.26.0

# Reasoning / analysis / creativity (wave 2)
plotly>=5.20.0
SQLAlchemy>=2.0.0
genanki>=0.13.0
bandit>=1.7.0

# Optional: run_python_snippet uses this if a Docker daemon is reachable,
# otherwise falls back to a plain subprocess automatically.
docker>=7.0.0

# Voice (Raspberry Pi), optional — install with: pip install -r requirements-voice.txt
